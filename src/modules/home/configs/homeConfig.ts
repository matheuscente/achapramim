import type { SelectModuleProps } from "@/modules/home/types/types";

const cepConfigs: SelectModuleProps = {
    button: "button",
    routeName: "CEPSearch"
}

const dddConfigs: SelectModuleProps = {
    button: "button",
    routeName: "DDDSearch"
}

export {
    dddConfigs,
    cepConfigs
}